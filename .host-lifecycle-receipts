[receipt "classify"]
    disposition = done
    evidence = case a: no prior CLAUDE.md; draft rename map empty (only LICENSE tracked)
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-03

[receipt "embed" "adlib_gold"]
    disposition = done
    evidence = adlib_gold@d898b8fe896e materialized at software/adlib_gold/main; pinned in .host-software
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-03

[receipt "remap"]
    disposition = skip
    reason = no ordinal-named files to rename; host-lint --all clean, so no .host-remap dictionary is needed
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-03

[receipt "publish"]
    disposition = done
    evidence = host mdBook generated, 15 pages, every room renders; .github/workflows/mdbook.yml wired
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-03

[receipt "release" "adlib_gold"]
    disposition = skip
    reason = no release performed this session; adlib_gold is repro-exempt (call/0002) and built off-host with the Windows WDK
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-03

[receipt "verify"]
    disposition = done
    evidence = gate sweep green: validate plan/ call/ ok, host-lint --all + prose + reconcile clean, book --check renders every room, mdbook build ok, commit-msg hook blocks a tell
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-03

[receipt "publish"]
    disposition = done
    evidence = host mdBook regenerated: 22 pages, every room renders (cast, plan, software, call, reference, memory); .github/workflows/mdbook.yml wired
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-04

[receipt "verify"]
    disposition = done
    evidence = gate sweep green over the current rooms (5 personas, 3 milestones, 8 decisions): validate plan/ call/ ok; host-lint --all, prose, reconcile clean; book --check renders every room; mdbook build ok; commit-msg hook blocks a tell
    tool = host-lifecycle@0.35.1
    recorded = 2026-07-04

[receipt "verify"]
    disposition = done
    evidence = gate sweep green after the upgrade: validate, host-lint --all, prose, reconcile, book --check all clean; software --check no hazard; the Where worktree re-materialized to the .bare layout
    tool = host-lifecycle@0.36.0
    recorded = 2026-07-04

[receipt "publish"]
    disposition = done
    evidence = book renders every room (22 pages) after the upgrade; mdbook.yml on checkout v5 and host-lifecycle 7e63d99
    tool = host-lifecycle@0.36.0
    recorded = 2026-07-04

[receipt "release" "adlib_gold"]
    disposition = skip
    reason = no release cut this session; the driver builds reproducibly (call/0015) but no versioned tag was cut
    tool = host-lifecycle@0.36.0
    recorded = 2026-07-04

[receipt "verify"]
    disposition = done
    evidence = gate sweep green after upgrade to host-template 4c6176f (baseline 7127dc4): validate, host-lint --all (2 advisory domain numerals only, 0 blocking), prose, reconcile, book --check (22 pages) all clean; software --check no HAZARD (worktree at pin, linux artifact 3f70ba6f verified, deps-bundle matches lock, allium+obligations+specula lanes present); tools/host-lifecycle aligned 7e63d99->bf0eacd (v0.37.0) to match the installed binary
    tool = host-lifecycle@0.37.0
    recorded = 2026-07-06
